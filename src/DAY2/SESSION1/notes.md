# Functions
    #Type of Functions
    1. In-build functions
    2. User defined:
        - Functions with return and without 
            parameters
        - Functions with return and with parameters
        - Functions without return and with parameters
        - Functions without return and without parameters


# Recursion 
    #Call Stack
    - Call stack is memory frame
    - push() and pop()
    - The no of function calls depends 
        on the 'n' value

    # Types of Recursion 
    1. Direct Recursion
    2. Indirect Recursion
    3. Tree Recursion
    4. Nested Recursion
    5. Head Recursion 
    6. Tail Recursion

# Class
    1. used in java as it can have functions 
        while the structures cannot 
    2. Class can be created inside the main class as well as outside
    3. Outside the main class , the class cannot be public
    4. Outside the main class the class can be protected or default

